const Player = require('../models/player');
const Coach = require('../models/coach');
const Admin = require('../models/admin');


function getPlayer(userID) {
    console.log(userID);
    return undefined;
}


function getCoach(userID) {
    console.log(userID);
    return undefined;
}


function getAdmin(userID) {
    console.log(userID);
    return undefined;
}
